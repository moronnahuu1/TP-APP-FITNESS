export class day{
    number: number;
    dayName: string;
    weekDay: number;
    constructor(number: number, dayName: string, weekDay: number){
        this.number = number;
        this.dayName = dayName;
        this.weekDay = weekDay;
    }
}